// function postTrimmer(req, res, next) {
//   if (req.method === 'POST') {
//     for (const [key, value] of Object.entries(req.body.data)) {
//       if (typeof value === 'string') req.body.data[key] = value.trim();
//     }
//   }
//   next();
// }

// module.exports = { postTrimmer };

// quotesentid:1,
// GUID:1,
// entmasterid:1,
// workspaceid:1,
// workspacename:1,
// title:1,
// tandc:1,
// counterid:1,
// commontax:1,
// itemcount:1,
// totalitemvalue:1,
// grandtotal:1,
// description:1,
// countrycode:1,
// currencycode:1,
// quotedate:1,
// expirydate:1,
// isread:1,
// readat:1,
// quotevalue:1,
// createdat:1,
// fromaddress:1,
// toaddress:1,
